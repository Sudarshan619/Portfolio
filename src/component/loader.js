import React from 'react';
import Loader from "react-js-loader";

const Loaders = () => (
    <div className={"item"}>
        <Loader type="spinner-cub" bgColor={"black"} color={"black"} title={"Loading..."} size={50} />
    </div>
);

export default Loaders;
